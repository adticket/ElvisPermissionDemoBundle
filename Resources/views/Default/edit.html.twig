{% extends "AdticketElvisCoreBundle::layout.html.twig" %}{% block title "Elvis-Berechtigungssystem Demo" %}

{% block content %}

    {% include "AdticketElvisPermissionDemoBundle:Default:nav.html.twig" %}

    <p>Um diese Seite anzuzeigen, muss der aktuelle Nutzer über das Recht <code>ELVISMODULE_PERMISSIONDEMO_EDIT</code> verfügen.</p>

    <h2>Formular-Demo</h2>

    <form action="{{ app.request.get('REQUEST_URI') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
    </form>

{% endblock %}
